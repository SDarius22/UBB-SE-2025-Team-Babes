<UserControl
    x:Class="SocialApp.Components.PostComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialApp.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="400">

    <Border BorderBrush="#393E46" BorderThickness="1" Padding="10" Background="#393E46">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" FontFamily="Segoe Script" Grid.Column="0" Text="Post Title" FontSize="18" FontWeight="Bold" VerticalAlignment="Top" Margin="0,0,10,0"/>

            <!-- Visibility Tag -->
            <Border Grid.Row="0" Grid.Column="1" Background="#D9D9D9" CornerRadius="5" Padding="5" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,10,0">
                <TextBlock Text="Public" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black"/>
            </Border>

            <!-- User Photo -->
            <Border 
                x:Name="User"
                Grid.Row="0"
                Grid.Column="2"
                Width="40" 
                Height="40" 
                CornerRadius="20"
                HorizontalAlignment="Right">
                <Image 
                    Source="../Assets/User.png" 
                    Stretch="UniformToFill"
                    HorizontalAlignment="Center"/>
            </Border>

            <!-- Text/Photo Field -->
            <ScrollViewer Grid.Row="1" Grid.ColumnSpan="3" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                <TextBox Text="Post content goes here..." TextWrapping="Wrap" AcceptsReturn="True" Background="#D9D9D9" Foreground="Black"/>
            </ScrollViewer>

            <!-- Reaction Buttons -->
            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
                <Button Content="👍" Width="40" Height="40" Margin="5" Background="#D9D9D9" CornerRadius="20"/>
                <Button Content="❤️" Width="40" Height="40" Margin="5" Background="#D9D9D9" CornerRadius="20"/>
                <Button Content="😂" Width="40" Height="40" Margin="5" Background="#D9D9D9" CornerRadius="20"/>
                <Button Content="😡" Width="40" Height="40" Margin="5" Background="#D9D9D9" CornerRadius="20"/>
            </StackPanel>

            <!-- Comment Button -->
            <Button Grid.Row="2" Grid.Column="2" Content="💀" Width="40" Height="40" HorizontalAlignment="Right" Margin="0,10,0,0" Background="#D9D9D9" CornerRadius="20"/>

            <!-- Time Since Post -->
            <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Text="{Binding TimeSincePost}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,10,0,0"/>
        </Grid>
    </Border>
</UserControl>
